const cardRentStyles = (theme) => ({
    filters: {
        display: "grid",
        gridTemplateColumns: "100px 1fr",
        paddingTop: "100px",
        marginBottom: "10px",
    }
})

export default cardRentStyles;